{
  "dependencies": {
    "parcel": "^2.15.4",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  }
  "scripts": {
  "start": "parcel public/index.html",
  "build": "parcel build public/index.html --dist-dir dist --public-url ./",
  "vercel-build": "parcel build public/index.html --dist-dir dist --public-url ./"
}

}
